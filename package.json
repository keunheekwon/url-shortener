{
  "name": "url-shortener",
  "version": "1.0.0",
  "main": "build/app.ts",
  "scripts": {
    "build": "rm -rf build && tsc",
    "start": "node build/app.js",
    "dev": "npm run build && npm run start"
  },
  "dependencies": {
    "crypto": "^1.0.1",
    "express": "^4.17.1",
    "mongoose": "^5.10.9",
    "dotenv": "^8.2.0"
  },
  "devDependencies": {
    "typescript": "^4.1.2",
    "ts-node-dev": "^1.0.0"
  }
}
